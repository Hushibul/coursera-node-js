let rect = {
  perimeter: (x, y) => 2 * (x + y),
  area: (x, y) => x * y,
};

function solveReactangel(l, b) {
  console.log(`Solving the reactangle with ${l} and ${b}`);

  if (l <= 0 || b <= 0) {
    console.log(`Dimensin should be greater than zero ${l} and ${b}`);
  } else {
    console.log(`The area of the rectangle is ${rect.area(l, b)}`);
    // console.log(`The perimeter of the rectangle is ${rect.perimeter(l, b)}`);
  }
}

solveReactangel(2, 5);
solveReactangel(3, 4);
solveReactangel(0, 5);
solveReactangel(-5, 5);
